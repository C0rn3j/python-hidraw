# hidraw-pure

Pythonic binding for Linux's hidraw ioctls.

Python 3.10+ required, use version 1.2 if support for older Python versions is needed

Note that the 1.2 version's module was named `hidraw`, but the current name is `hidraw_pure`, as the former clashed with hidapi.
